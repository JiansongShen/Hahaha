name: CI

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev, main ]

jobs:
  build-and-test:
    uses: ./.github/workflows/_build_and_test.yml

  coverage:
    needs: build-and-test
    uses: ./.github/workflows/_coverage.yml
